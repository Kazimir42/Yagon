'use client'

// @ts-ignore
export default function DashboardLayout({children}) {
    return (
        <html lang="en">
            <body className={'font-sans bg-main'}>{children}</body>
        </html>
    )
}
