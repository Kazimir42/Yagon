'use client'

// @ts-ignore
export default function ProductLayout({children}) {
    return (
        <html lang="en">
            <body className={'font-sans bg-main'}>{children}</body>
        </html>
    )
}
